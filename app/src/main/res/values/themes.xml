<resources>
    <style name="SLPTheme" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:colorPrimary">@color/background</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:textColor">@color/textColor</item>

        <item name="colorPrimary">@color/green</item>
        <item name="colorOnPrimary">@color/green</item>
        <item name="colorContainer">@color/green</item>

        <item name="popupMenuBackground">@drawable/menu_background</item>
    </style>

</resources>